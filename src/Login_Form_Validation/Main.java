package Login_Form_Validation;

import javax.swing.*;

public class Main{
	public static void main(String[] args){
		MAIN_WINDOW app = new MAIN_WINDOW();
		app.setContentPane(app.getContentPane());
		app.setDefaultCloseOperation(WindowConstants.EXIT_ON_CLOSE);
		app.setSize(300,300);
		app.setVisible(true);
	}
}
